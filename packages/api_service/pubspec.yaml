name: api_service
description: API service for backend communication
version: 1.0.0

environment:
  sdk: '>=2.17.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_dotenv: ^5.0.2
  flutter_secure_storage: ^9.0.0